# idf_component.yml
#
# Causes 'menuconfig' to activate the same external components as in Rust build.
#
# NOTE:
#   Keep this aligned with '../raw/Cargo.toml':
#   <<
#     [[package.metadata.esp-idf-sys.extra_components]]
#     remote_component = { name = "espressif/esp-zigbee-lib", version = "1.*" }
#
#     [[package.metadata.esp-idf-sys.extra_components]]
#     remote_component = { name = "espressif/esp-zboss-lib", version = "1.*" }
#   <<
#
# References:
#   - 'idf_component.yml' Manifest File (Espressif docs)
#     -> https://docs.espressif.com/projects/idf-component-manager/en/latest/reference/manifest_file.html
#
targets:
  - esp32c6

dependencies:
  espressif/esp-zigbee-lib: "1.*"
  espressif/esp-zboss-lib: "1.*"
  idf: ">=5.5"
