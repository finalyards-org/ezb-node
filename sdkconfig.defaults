#
# sdkconfig.defaults
#
# NOTE! If you misspell a config (or use one you only dream existed), ESP-IDF build chain will **warn**, but not stop
#     the build! Keep an eye on the build output!!

# Rust needs more main task stack size compared to C
#   - C 'esp_zigbee_sdk' examples use 4096
#
CONFIG_ESP_MAIN_TASK_STACK_SIZE=20000

# From 'esp-idf-svc', with mention that these be needed at least if (as we do) 'embassy-time-driver' is being used.
CONFIG_ESP_TIMER_TASK_STACK_SIZE=4096
#CONFIG_ESP_MAIN_TASK_STACK_SIZE=20000
CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=8192
CONFIG_PTHREAD_TASK_STACK_SIZE_DEFAULT=8192

# Use this to set FreeRTOS kernel tick frequency to 1000 Hz (100 Hz by default).
# This allows to use 1 ms granularity for thread sleeps (10 ms by default).
#CONFIG_FREERTOS_HZ=1000


#---
# Below, components seen as unimportant for Zigbee applications are disabled.
#
# NOTE: THIS NEEDS MORE WORK. 'sdkconfig.defaults' are just _defaults_; we cannot switch
#       off things activated elsewhere from here.
#|#
#|# WiFi
#|CONFIG_ESP_WIFI_ENABLED=n      # becomes 'true'
#|CONFIG_ESP_WIFI_SOFTAP_SUPPORT=n
#|#R CONFIG_ESP_WIFI_STA_SUPPORT=n  # NO SUCH
#|CONFIG_ESP_COEX_ENABLED=n       # __
#|
#|# Bluetooth
#|CONFIG_BT_ENABLED=n             # ?
#|#CONFIG_BT_BLE_ENABLED=n        # NO SUCH
#|#CONFIG_BT_CLASSIC_ENABLED=n    # NO SUCH
#|
#|# Thread
#|CONFIG_OPENTHREAD_ENABLED=n     # ?
#|
#|# http server
#|#CONFIG_ESP_HTTP_SERVER=n       # NO SUCH
#|#CONFIG_MBEDTLS=n               # LOADS of other 'MBEDTLS' - NO SUCH
